info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/plans/:plan_id"
  params:
    - name: plan_id
      value: cd7e9f5c-ccea-4412-82de-ea171bc41b75
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
